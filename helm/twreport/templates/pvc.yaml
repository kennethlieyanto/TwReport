apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvc.taskData }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "twreport.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
